/**
 * Integration tests for the Overpass / POI REST API.
 *
 * Covers /api/overpass and /api/poi (geo-lookup proxy endpoints).
 *
 * No tests exist yet — this file is a placeholder for future coverage.
 *
 * Run (in-process):
 *   pnpm --filter @hideandseek/backend test
 */
import { describe, it } from "vitest";

// Placeholder — no tests yet for /api/overpass or /api/poi.
describe("Overpass / POI", () => {
    it.todo("GET /api/overpass returns results for a given bbox");
});

