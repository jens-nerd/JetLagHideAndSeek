/**
 * Integration tests for the Uploads REST API.
 *
 * Covers /api/upload (photo upload endpoint).
 *
 * No tests exist yet — this file is a placeholder for future coverage.
 *
 * Run (in-process):
 *   pnpm --filter @hideandseek/backend test
 */
import { describe, it } from "vitest";

// Placeholder — no tests yet for /api/upload.
describe("Uploads", () => {
    it.todo("POST /api/upload accepts a multipart photo and returns a URL");
});

